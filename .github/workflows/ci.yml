name: CI

on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:
  tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine

        env:
          POSTGRES_DB: home_space_calculator_test
          POSTGRES_USER: homespace_user
          POSTGRES_PASSWORD: homespace_password

        ports:
          - 5432:5432

        options: >-
          --health-cmd="pg_isready -U homespace_user"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DJANGO_SECRET_KEY: github-actions-test-secret-key
      DJANGO_DEBUG: "False"

      DB_NAME: home_space_calculator_test
      DB_USER: homespace_user
      DB_PASSWORD: homespace_password
      DB_HOST: localhost
      DB_PORT: "5432"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django system check
        run: python manage.py check

      - name: Run migrations
        run: python manage.py migrate

      - name: Run tests
        run: python manage.py test